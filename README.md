# Krrish Jha — AI/ML Portfolio (React + Vite)

Premium dark, glassmorphism one-page portfolio with Tailwind CSS + Framer Motion.

## Tech

- React + Vite
- Tailwind CSS
- Framer Motion
- lucide-react

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

## Resume

Your resume is served from:

- `public/resume.pdf`

The **View Resume** button in the Hero links to `/resume.pdf`.

## GitHub links (required)

Add your real GitHub URL / repo links in:

- `src/components/Hero.jsx` (`GITHUB_URL`)
- `src/components/Footer.jsx` (`GITHUB_URL`)
- `src/components/Projects.jsx` (each project `github`)

# Krrish_Jha
# Krrish_Jha
